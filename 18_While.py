# Condicion de ejecucion
contador = 1;

while (contador <= 5):
    print("Mensaje #",contador) # Código a ejecutar mientras se cumpla la condición
    contador += 1
    if (contador == 3) : # Condicion forzada del while
        break;
      # continue - instrucion para pasar a realizar otra instruccion diferente
